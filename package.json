{
  "name": "sergey-xursevich",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "install": "lerna run prepublish",
    "pub": "lerna publish --no-private"
  },
  "devDependencies": {
    "babel-preset-react-app": "^10.0.1",
    "cross-env": "^7.0.3",
    "lerna": "^8.0.0"
  }
}
